##  시퀀스 간단 예제 ##
KH_MEMBER 테이블을 생성
컬럼
MEMBER_ID		NUMBER
MEMBER_NAME		VARCHAR2(20)
MEMBER_AGE		NUMBER
MEMBER_JOIN_COM	NUMBER

이때 해당 사원들의 정보를 INSERT 해야 함
ID 값과 JOIN_COM은 SEQUENCE 를 이용하여 정보를 넣고자 함

SEQUENCE 이름은 KH_MEMBER_ID_SEQ, KH_MEMBER_JOIN_COM_SEQ, 
ID값은 500 번 부터 시작하여 10씩 증가하여 저장 하고자 함
JOIN_COM 값은 1번부터 시작하여 1씩 증가하여 저장 해야 함
(ID 값과 JOIN_COM 값의 MAX는 10000으로 설정)


데이터 삽입후 SELECT 결과 확인

	MEMBER_ID	MEMBER_NAME	MEMBER_AGE	MEMBER_JOIN_COM	
	500		홍길동		20		1
	510		김말똥		30		2
	520		삼식이		40		3
	530		고길똥		24		4




@ 정답코드 (맨 아래부터 보세요)

SELECT * FROM KH_MEMBER;

INSERT INTO KH_MEMBER VALUES(
KH_MEMBER_ID_SEQ.NEXTVAL, '홍길동',20, KH_MEMBER_JOIN_COM_SEQ.NEXTVAL);

INSERT INTO KH_MEMBER VALUES(
KH_MEMBER_ID_SEQ.NEXTVAL, '김말똥',30, KH_MEMBER_JOIN_COM_SEQ.NEXTVAL);

INSERT INTO KH_MEMBER VALUES(
KH_MEMBER_ID_SEQ.NEXTVAL, '삼식이',40, KH_MEMBER_JOIN_COM_SEQ.NEXTVAL);

INSERT INTO KH_MEMBER VALUES(
KH_MEMBER_ID_SEQ.NEXTVAL, '고길똥',24, KH_MEMBER_JOIN_COM_SEQ.NEXTVAL);





SELECT * FROM KH_MEMBER;


CREATE SEQUENCE KH_MEMBER_JOIN_COM_SEQ
START WITH 1
INCREMENT BY 1
MAXVALUE 10000;


CREATE SEQUENCE KH_MEMBER_ID_SEQ
START WITH 500
INCREMENT BY 10
MAXVALUE 10000
;


CREATE TABLE KH_MEMBER
(
    MEMBER_ID NUMBER,
    MEMBER_NAME VARCHAR2(20),
    MEMBER_AGE NUMBER,
    MEMBER_JOIN_COM NUMBER
);















